(define str "Hello World!")
(print str)
# The lexer ignores anything in the quotations, just looks for the next "
# Then saves that as a type -3 token
(define str2 "")
# comments don't need a SOL token
(define x 2)
(define z (+ x 3))
(print z)
(print (++ z))

# a function
(lambda func1 (x y) (* 2 (- y x)))
(func1 2 4) # resolves to: 2 * (4 - 2) = 4
(exit)